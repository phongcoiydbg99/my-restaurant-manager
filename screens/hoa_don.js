import React from "react";
import {
  StyleSheet
} from "react-native";
import TabBill from "../config/tabBill";
export default () => (
  <TabBill />
);
const styles = StyleSheet.create({
  container: {
    flex: 1,
    width: "100%",
    height: "100%",
  },
});
